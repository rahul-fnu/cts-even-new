{
  "name": "claude-token-saver",
  "version": "1.0.0",
  "description": "CLI tool for managing Claude token usage",
  "type": "module",
  "bin": {
    "claude-token-saver": "./dist/index.js",
    "cts": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
